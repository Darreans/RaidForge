using BepInEx.Configuration;
using BepInEx.Logging;
/*
namespace RaidForge.Config
{
    public static class ExplosivesConfig
    {
        public static ConfigEntry<bool> EnableExplosiveOverhaul { get; private set; }
        public static ConfigEntry<float> ExplosiveStructureDamageMultiplier { get; private set; }

        public static void Initialize(ConfigFile configFile, ManualLogSource logger = null)
        {
            EnableExplosiveOverhaul = configFile.Bind("Explosives", "EnableExplosiveOverhaul", true, "Enable RaidForge control over explosive damage.");
            ExplosiveStructureDamageMultiplier = configFile.Bind("Explosives", "ExplosiveStructureDamageMultiplier", 1.0f, "Damage multiplier for explosives vs walls (1.0 = normal) will allow breaching on walls .");

            if (logger != null) logger.LogInfo("[ExplosivesConfig] Initialized.");
        }
    }
}

*/